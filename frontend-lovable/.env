# Backend API base URL (must end with /api)
# Health is at origin/health, e.g. http://localhost:3000/health
#VITE_API_URL=http://localhost:3000/api/v1
VITE_API_URL=https://easytestapi.simc.com.br/api/v1

# ------------------------------------------------------------------------------
# Regra de ouro: frontend não pode travar
# ------------------------------------------------------------------------------
# Número máximo de tentativas "Tentar novamente" antes de fazer logout e voltar ao login
VITE_MAX_RECOVERY_RETRIES=3

# Ambiente: production | development (afeta mensagens e comportamento)
VITE_APP_ENV=development

# Debug: só quando true os console.log de debug são exibidos (use false em produção)
VITE_DEBUG=true

# ------------------------------------------------------------------------------
# Dev server do frontend (Node/Vite) — usado por npm run dev
# ------------------------------------------------------------------------------
# Onde escutar: "::" = todas as interfaces
VITE_HOST=::
# Hosts permitidos (além de localhost/127.0.0.1). Vários separados por vírgula.
VITE_ALLOWED_HOSTS=easytest.simc.com.br
VITE_PORT=8080

# Quando o backend está indisponível: redirecionar para /login após este número de segundos (0 = desliga)
VITE_BACKEND_DOWN_REDIRECT_SECONDS=30
